---
name: review
description: |
  Run a structured code review on staged changes, a PR, or a branch. Triggers on:
  "review this code", "code review", "review my changes".
---

# Hive Review

Run a structured code review workflow.

**Input:** `$ARGUMENTS` optionally contains a PR number (`#123`), branch name, or file paths.

**Instructions:** Read the full Hive skill at `hive/ORCHESTRATOR.md` and follow the **Review Command** section. All shared resources are in the `hive/` directory.
