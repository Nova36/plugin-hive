---
name: execute
description: |
  Execute a planned epic's stories through development workflows. Supports
  --methodology (tdd, classic, bdd) and --sequential flags. Triggers on:
  "execute the epic", "run the workflow", "start implementation".
---

# Hive Execute

Execute stories through development workflow phases.

**Input:** `$ARGUMENTS` contains the epic ID and optional flags (`--methodology tdd|classic|bdd`, `--sequential`).

**Instructions:** Read the full Hive skill at `hive/ORCHESTRATOR.md` and follow the **Execute Command** section. All shared resources are in the `hive/` directory.
